import{Lt as e,Pt as t,St as n,Z as r,bt as i,ht as a,vt as o}from"./app-BkR7hyOa.js";var s=JSON.parse(`{"path":"/blog/signing-upm-packages-with-openupm/","title":"Signing UPM Packages with OpenUPM","lang":"en-US","frontmatter":{"title":"Signing UPM Packages with OpenUPM","author":"Favo Yang","date":"2026-05-08","readingTime":"3 min read","description":"Unity 6.3 package signing lets package authors ship signed tarballs through GitHub Releases while OpenUPM republishes the exact artifact.","editLink":false,"cover":"/images/blog-signing-upm-packages-code-sign.png","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Signing UPM Packages with OpenUPM\\",\\"image\\":[\\"https://openupm.com/images/blog-signing-upm-packages-code-sign.png\\"],\\"datePublished\\":\\"2026-05-08T00:00:00.000Z\\",\\"dateModified\\":null,\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"Favo Yang\\"}]}"],["meta",{"property":"og:url","content":"https://openupm.com/blog/signing-upm-packages-with-openupm/"}],["meta",{"property":"og:site_name","content":"OpenUPM"}],["meta",{"property":"og:title","content":"Signing UPM Packages with OpenUPM"}],["meta",{"property":"og:description","content":"Unity 6.3 package signing lets package authors ship signed tarballs through GitHub Releases while OpenUPM republishes the exact artifact."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://openupm.com/images/blog-signing-upm-packages-code-sign.png"}],["meta",{"property":"og:locale","content":"en-US"}],["meta",{"name":"twitter:card","content":"summary_large_image"}],["meta",{"name":"twitter:image:src","content":"https://openupm.com/images/blog-signing-upm-packages-code-sign.png"}],["meta",{"name":"twitter:image:alt","content":"Signing UPM Packages with OpenUPM"}],["meta",{"property":"article:author","content":"Favo Yang"}],["meta",{"property":"article:published_time","content":"2026-05-08T00:00:00.000Z"}],["link",{"rel":"canonical","href":"https://openupm.com/blog/signing-upm-packages-with-openupm/"}]]},"filePathRelative":"blog/signing-upm-packages-with-openupm/index.md"}`),c={name:`index.md`};function l(r,s,c,l,u,d){let f=e(`BlogPostMeta`),p=e(`BlogPostNav`);return t(),o(`div`,null,[s[0]||=a(`h1`,{id:`signing-upm-packages-with-openupm`,tabindex:`-1`},[a(`a`,{class:`header-anchor`,href:`#signing-upm-packages-with-openupm`},[a(`span`,null,`Signing UPM Packages with OpenUPM`)])],-1),n(f),s[1]||=i(`<p>Unity 6.3 introduced package signature verification for Unity Package Manager packages. For package authors and Unity developers, this is an important step toward clearer package provenance: the package can now be signed by the developer who builds it, instead of relying entirely on the registry that republishes it.</p><p>OpenUPM supports this model by publishing signed package tarballs that package authors build and attach to GitHub Releases.</p><h2 id="why-this-matters" tabindex="-1"><a class="header-anchor" href="#why-this-matters"><span>Why This Matters</span></a></h2><p>Traditionally, OpenUPM discovers a package version from a Git tag, clones the repository, and packs the package for publishing. That works well for many packages, but it is not enough when the package needs release-time work such as signing, generating files, compiling DLLs, or including native binaries.</p><p>With signed packages, the package author owns the full release process. They build the <code>.tgz</code>, sign it with Unity&#39;s UPM CLI, and upload the final artifact to GitHub Releases. OpenUPM then downloads and republishes that exact tarball unchanged.</p><p>This keeps trust closer to the package author. It also allows signed packages to avoid unsigned package warnings in the Unity Package Manager UI.</p><h2 id="how-the-workflow-works" tabindex="-1"><a class="header-anchor" href="#how-the-workflow-works"><span>How The Workflow Works</span></a></h2><p>The package author creates a CI workflow, usually with GitHub Actions. For every version tag, the workflow should:</p><ol><li>Check out the repository.</li><li>Install Unity&#39;s UPM CLI.</li><li>Run <code>upm pack</code> for the package folder.</li><li>Sign the package using Unity service account credentials.</li><li>Verify the tarball includes <code>package/package.json</code>.</li><li>Verify the tarball includes <code>package/.attestation.p7m</code>.</li><li>Create a GitHub Release for the same tag.</li><li>Upload the signed <code>.tgz</code> or <code>.tar.gz</code> file as a release asset.</li></ol><p>The release asset is important. GitHub Actions artifacts and logs expire, but GitHub Release assets remain attached to the release unless deleted. That gives OpenUPM a stable artifact to publish, including when older versions need to be processed later.</p><h2 id="openupm-configuration" tabindex="-1"><a class="header-anchor" href="#openupm-configuration"><span>OpenUPM Configuration</span></a></h2><p>To use GitHub Release assets instead of packing directly from Git, package metadata should use:</p><div class="language-yaml" data-highlighter="prismjs" data-ext="yml"><pre><code class="language-yaml"><span class="line"><span class="token key atrule">trackingMode</span><span class="token punctuation">:</span> githubRelease</span>
<span class="line"></span></code></pre></div><p>If a release contains only one publishable <code>.tgz</code> or <code>.tar.gz</code> asset, OpenUPM can select it automatically.</p><p>If there are multiple assets, the package can specify an exact filename or stable prefix:</p><div class="language-yaml" data-highlighter="prismjs" data-ext="yml"><pre><code class="language-yaml"><span class="line"><span class="token key atrule">trackingMode</span><span class="token punctuation">:</span> githubRelease</span>
<span class="line"><span class="token key atrule">githubReleaseAssetName</span><span class="token punctuation">:</span> <span class="token string">&#39;com.example.package-&#39;</span></span>
<span class="line"></span></code></pre></div><p>The prefix should not include a version number, otherwise the metadata would need to change for every release. The asset name is a filename only, not a path inside the repository or CI workspace.</p><h2 id="migrating-existing-packages" tabindex="-1"><a class="header-anchor" href="#migrating-existing-packages"><span>Migrating Existing Packages</span></a></h2><p>Since Unity introduced package signature verification in Unity 6.3, existing packages are not expected to have signed historical versions. The practical path is to start signing the next version you release.</p><p>For packages that are already listed on OpenUPM, change the metadata to <code>trackingMode: githubRelease</code> first and wait until that pull request is merged. Only then bump the package version and push the new tag. This avoids a race where the existing Git tracker sees the new version first and publishes a tarball packed from the Git checkout instead of the signed GitHub Release asset.</p><h2 id="example-repository" tabindex="-1"><a class="header-anchor" href="#example-repository"><span>Example Repository</span></a></h2><p>OpenUPM provides a minimal signed package example at:</p><div class="language-text" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">https://github.com/openupm/com.example.signed-upm</span>
<span class="line"></span></code></pre></div><p>Package authors can use that repository to learn the details of the workflow, including the package layout, GitHub Actions release process, Unity service account secret names, and GitHub Release asset setup.</p><h2 id="what-signing-does-and-does-not-prove" tabindex="-1"><a class="header-anchor" href="#what-signing-does-and-does-not-prove"><span>What Signing Does And Does Not Prove</span></a></h2><p>Package signing improves provenance, but it is not a malware scanner. It does not prove that the source code, generated files, build scripts, or dependencies are safe.</p><p>It proves that the signed artifact came through the author&#39;s signing workflow. For higher-security projects, teams should still review the source, dependencies, release workflow, and final published tarball.</p><h2 id="when-to-use-this" tabindex="-1"><a class="header-anchor" href="#when-to-use-this"><span>When To Use This</span></a></h2><p>Use the default Git tag tracking when OpenUPM can safely pack the package directly from the tagged repository.</p><p>Use GitHub Release asset tracking when the final package must be produced by your own release workflow. This is the right choice for signed packages, generated builds, DLLs, native binaries, or any package that should be published as a prepared artifact rather than a raw Git checkout.</p><p>OpenUPM&#39;s role remains simple: discover the version, fetch the matching release asset, and publish the tarball unchanged. The author keeps control of signing credentials, build logic, and the final package content.</p><p>For the complete guide, read <a href="/docs/signing-upm-packages">Signing UPM Packages</a>.</p>`,32),n(p)])}var u=r(c,[[`render`,l]]);export{s as _pageData,u as default};