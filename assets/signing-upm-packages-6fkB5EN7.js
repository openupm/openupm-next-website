import{Ht as e,Lt as t,Pt as n,St as r,Z as i,bt as a,ht as o,vt as s,xt as c}from"./app-BkR7hyOa.js";var l=JSON.parse(`{"path":"/docs/signing-upm-packages.html","title":"Signing UPM Packages","lang":"en-US","frontmatter":{"description":"Signing UPM Packages Unity 6.3 introduced package signature verification for Unity Package Manager packages. OpenUPM supports signed packages by consuming signed tarballs that p...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Signing UPM Packages\\",\\"image\\":[\\"https://openupm.com/images/openupm-twitter.png\\"],\\"dateModified\\":null,\\"author\\":[]}"],["meta",{"property":"og:url","content":"https://openupm.com/docs/signing-upm-packages.html"}],["meta",{"property":"og:site_name","content":"OpenUPM"}],["meta",{"property":"og:title","content":"Signing UPM Packages"}],["meta",{"property":"og:description","content":"Signing UPM Packages Unity 6.3 introduced package signature verification for Unity Package Manager packages. OpenUPM supports signed packages by consuming signed tarballs that p..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://openupm.com/images/openupm-twitter.png"}],["meta",{"property":"og:locale","content":"en-US"}],["link",{"rel":"canonical","href":"https://openupm.com/docs/signing-upm-packages.html"}]]},"filePathRelative":"docs/signing-upm-packages.md","autoDesc":true}`),u={name:`signing-upm-packages.md`};function d(i,l,u,d,f,p){let m=t(`RouteLink`);return n(),s(`div`,null,[l[14]||=a(`<h1 id="signing-upm-packages" tabindex="-1"><a class="header-anchor" href="#signing-upm-packages"><span>Signing UPM Packages</span></a></h1><p>Unity 6.3 introduced package signature verification for Unity Package Manager packages. OpenUPM supports signed packages by consuming signed tarballs that package authors build and attach to GitHub Releases.</p><p>The main benefit is that the developer can sign the package directly. Trust no longer has to be centered on the package registry that republishes the package. For Unity Editor users, a signed package can also remove the unsigned package warning from the Package Manager UI.</p><p>OpenUPM does not sign packages on behalf of package authors. The package author owns the Unity organization, service account, build process, and release asset. OpenUPM&#39;s role is to discover the version tag, download the matching public GitHub Release asset, and publish that tarball unchanged.</p><p>Use this workflow when your package needs any of these release-time steps:</p><ul><li>signing the package with Unity&#39;s UPM CLI</li><li>building generated files, DLLs, or native binaries before publishing</li><li>publishing package content that should not be committed to a UPM branch</li><li>keeping a pull-based OpenUPM release flow without giving OpenUPM credentials or custom build logic</li></ul><h2 id="how-it-works" tabindex="-1"><a class="header-anchor" href="#how-it-works"><span>How It Works</span></a></h2><p>The signed package is created in your own CI workflow. For each version tag, the workflow builds a UPM <code>.tgz</code> package, signs it with Unity service account credentials, creates a GitHub Release for the same tag, and uploads the signed tarball as a release asset.</p><p>OpenUPM then uses <code>trackingMode: githubRelease</code> in the package metadata. Instead of cloning the tag and running <code>npm pack</code>, OpenUPM downloads the <code>.tgz</code> or <code>.tar.gz</code> asset from the GitHub Release whose tag name exactly matches the version tag.</p><p>Signed Unity packages contain <code>package/.attestation.p7m</code> inside the tarball. OpenUPM can show the package as signed when the published release asset contains that signing attestation.</p><h2 id="unity-s-signing-model" tabindex="-1"><a class="header-anchor" href="#unity-s-signing-model"><span>Unity&#39;s Signing Model</span></a></h2><p>Unity&#39;s current signing implementation uses a Unity service account and the UPM CLI. The CLI is lightweight enough to run in GitHub Actions, but the actual signing is backed by Unity. Unity owns the certificate and private keys, while the package author authenticates with service account credentials and chooses which package tarball to sign.</p><p>This model is convenient for package authors because they do not have to manage, store, rotate, or protect their own signing certificate and private key. It also lets Unity keep some control over the signing ecosystem while still allowing package developers to sign releases from their own CI.</p><h2 id="requirements" tabindex="-1"><a class="header-anchor" href="#requirements"><span>Requirements</span></a></h2><ul><li>a public GitHub repository that meets the normal OpenUPM package criteria</li><li>Unity UPM CLI available in CI</li><li>a Unity service account with package signing permission for the Unity organization that should sign the package</li><li>GitHub Actions secrets for the Unity service account</li><li>a GitHub Release asset ending in <code>.tgz</code> or <code>.tar.gz</code> for every published tag</li></ul><p>Use these GitHub Actions secrets for the signing credentials:</p><div class="language-text" data-highlighter="prismjs" data-ext="text"><pre><code class="language-text"><span class="line">UPM_SERVICE_ACCOUNT_KEY_ID</span>
<span class="line">UPM_SERVICE_ACCOUNT_KEY_SECRET</span>
<span class="line">UPM_ORG_ID</span>
<span class="line"></span></code></pre></div><p>Repository secrets are enough for one package. For several repositories that sign packages for the same Unity organization, GitHub organization secrets scoped to only those repositories are easier to rotate.</p><h2 id="github-actions-flow" tabindex="-1"><a class="header-anchor" href="#github-actions-flow"><span>GitHub Actions Flow</span></a></h2><p>The release workflow should run only for version tags:</p><div class="language-bash" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token function">git</span> tag <span class="token number">1.0</span>.0</span>
<span class="line"><span class="token function">git</span> push origin main <span class="token number">1.0</span>.0</span>
<span class="line"></span></code></pre></div><p>For each tag, the workflow should:</p><ol><li>check out the repository</li><li>install Unity UPM CLI</li><li>run <code>upm pack ./package</code> or the equivalent package folder</li><li>verify that the tarball contains <code>package/package.json</code></li><li>verify that the tarball contains <code>package/.attestation.p7m</code></li><li>create a GitHub Release whose tag is the same version tag</li><li>upload the signed package tarball as a release asset</li></ol><p>Upload the signed package as a GitHub Release asset, not only as a GitHub Actions workflow artifact. Workflow artifacts and logs expire after their retention period. Release assets stay attached to the release until the asset or release is deleted, so OpenUPM can process older versions later.</p>`,24),o(`p`,null,[l[2]||=c(`Optionally, for a more integrated release experience, call the `,-1),r(m,{to:`/docs/github-action-publish.html`},{default:e(()=>[...l[0]||=[c(`OpenUPM GitHub Action`,-1)]]),_:1}),l[3]||=c(` after the workflow publishes the GitHub Release and uploads the signed asset. The action waits until that version is available from the registry. Signed packages and other GitHub Release asset tracking packages should trigger OpenUPM only after the release asset is present. If the same GitHub Actions workflow creates the release, invoke `,-1),l[4]||=o(`code`,null,`openupm/openupm-action`,-1),l[5]||=c(` as a later step in that workflow. Use a separate `,-1),r(m,{to:`/docs/github-action-publish.html#github-release-workflows`},{default:e(()=>[...l[1]||=[c(`GitHub Release workflow`,-1)]]),_:1}),l[6]||=c(` only when the release event is created outside that workflow and the package asset is already attached when the event fires.`,-1)]),l[15]||=a(`<p>The <a href="https://github.com/openupm/com.example.signed-upm" target="_blank" rel="noopener noreferrer">Signed UPM Example</a> repository is a minimal reference implementation. Its <a href="https://github.com/openupm/com.example.signed-upm/blob/main/README.md" target="_blank" rel="noopener noreferrer">README</a> documents the current workflow and credential names.</p><h2 id="openupm-metadata" tabindex="-1"><a class="header-anchor" href="#openupm-metadata"><span>OpenUPM Metadata</span></a></h2><p>Set the package metadata to use GitHub Release asset tracking:</p><div class="language-yaml" data-highlighter="prismjs" data-ext="yml"><pre><code class="language-yaml"><span class="line"><span class="token key atrule">trackingMode</span><span class="token punctuation">:</span> githubRelease</span>
<span class="line"></span></code></pre></div><p>When a release has only one publishable <code>.tgz</code> or <code>.tar.gz</code> asset, OpenUPM selects it automatically. If a release has multiple assets, add <code>githubReleaseAssetName</code>:</p><div class="language-yaml" data-highlighter="prismjs" data-ext="yml"><pre><code class="language-yaml"><span class="line"><span class="token key atrule">trackingMode</span><span class="token punctuation">:</span> githubRelease</span>
<span class="line"><span class="token key atrule">githubReleaseAssetName</span><span class="token punctuation">:</span> <span class="token string">&#39;com.example.package-&#39;</span></span>
<span class="line"></span></code></pre></div><p><code>githubReleaseAssetName</code> can be either the exact asset filename or a stable filename prefix. The prefix should never contain a version number; otherwise the package metadata would need to change for every release. GitHub Release asset names are filenames only; do not include a directory path such as <code>dist/</code>. OpenUPM still discovers versions from Git tags in this mode. Normal build failures are retried up to three times. If the GitHub Release does not exist yet, or the publishable asset is not attached yet, OpenUPM keeps checking after the initial attempts are exhausted. The follow-up checks start at 10 minutes, then 20 minutes, then 40 minutes, doubling until they reach 6 hours. OpenUPM then checks every 6 hours for up to 3 days from the first missing-release or missing-asset failure. After 3 days, create the missing release or asset and request a rebuild.</p><p>Signed release tarballs are often built from a package folder and may contain only the package payload plus the signing attestation. The README does not have to be inside the tarball for the package itself to be signed and published.</p><h2 id="migrating-existing-packages" tabindex="-1"><a class="header-anchor" href="#migrating-existing-packages"><span>Migrating Existing Packages</span></a></h2><p>Because Unity introduced package signing in Unity 6.3, existing OpenUPM packages are not expected to have signed historical versions. A good migration path is to add signing for the next package version you release, then keep future releases signed from that point onward.</p><p>For an existing OpenUPM package, submit and merge the package metadata change that switches <code>trackingMode</code> from <code>git</code> to <code>githubRelease</code> before you publish a new signed version tag. Wait until that pull request is merged, then bump the package version and push the tag. This avoids a race where the current Git tracker detects the new version first and publishes a package packed from the Git checkout instead of the signed GitHub Release asset.</p><h2 id="limitations" tabindex="-1"><a class="header-anchor" href="#limitations"><span>Limitations</span></a></h2><p>Package signing improves package provenance, but it is not a malware scanner and it does not prove that the package source code is safe. The npm and JavaScript ecosystem has shown that a central registry&#39;s trust model is not the only risk: malicious code can still be introduced in source, build scripts, dependencies, or generated files before a package is signed.</p><p>GitHub Release asset tracking also means the shipped package tarball may not be identical to the repository source at the tag. A package author may build a DLL, generate files, strip development-only files, or otherwise transform the source before uploading the final tarball. This is the same kind of release model used by many npm packages: the source repository is important, but the published artifact is the package that users actually install.</p><p>Treat signing as one layer of trust. Review the package source, release workflow, dependencies, and published tarball when your project has higher security requirements.</p><h2 id="choosing-between-git-tags-and-release-assets" tabindex="-1"><a class="header-anchor" href="#choosing-between-git-tags-and-release-assets"><span>Choosing Between Git Tags And Release Assets</span></a></h2><p>Use the default <code>trackingMode: git</code> when OpenUPM can pack the package directly from the tagged Git checkout.</p>`,17),o(`p`,null,[l[8]||=c(`Use `,-1),l[9]||=o(`code`,null,`trackingMode: githubRelease`,-1),l[10]||=c(` when the published package must be produced by your own release workflow. This keeps signing credentials and custom build steps in your repository while still letting OpenUPM publish the final UPM package. Pair this mode with `,-1),l[11]||=o(`code`,null,`openupm/openupm-action`,-1),l[12]||=c(` after the release asset upload when you want CI to notify OpenUPM immediately. The `,-1),r(m,{to:`/docs/github-action-publish.html#github-release-workflows`},{default:e(()=>[...l[7]||=[c(`GitHub Release workflow`,-1)]]),_:1}),l[13]||=c(` is a good fit when the release is published before that workflow starts and its package asset is already available.`,-1)])])}var f=i(u,[[`render`,d]]);export{l as _pageData,f as default};