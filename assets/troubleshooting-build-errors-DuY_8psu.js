import{Pt as e,Z as t,bt as n,vt as r}from"./app-BkR7hyOa.js";var i=JSON.parse(`{"path":"/docs/troubleshooting-build-errors.html","title":"Troubleshooting Build Errors","lang":"en-US","frontmatter":{"description":"Troubleshooting Build Errors The build pipelines section on the package detail page reports the build status of each versioned Git tag, allowing you to check for any failed buil...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Troubleshooting Build Errors\\",\\"image\\":[\\"https://openupm.com/images/openupm-twitter.png\\"],\\"dateModified\\":null,\\"author\\":[]}"],["meta",{"property":"og:url","content":"https://openupm.com/docs/troubleshooting-build-errors.html"}],["meta",{"property":"og:site_name","content":"OpenUPM"}],["meta",{"property":"og:title","content":"Troubleshooting Build Errors"}],["meta",{"property":"og:description","content":"Troubleshooting Build Errors The build pipelines section on the package detail page reports the build status of each versioned Git tag, allowing you to check for any failed buil..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://openupm.com/images/openupm-twitter.png"}],["meta",{"property":"og:locale","content":"en-US"}],["link",{"rel":"canonical","href":"https://openupm.com/docs/troubleshooting-build-errors.html"}]]},"filePathRelative":"docs/troubleshooting-build-errors.md","autoDesc":true}`),a={name:`troubleshooting-build-errors.md`};function o(t,i,a,o,s,c){return e(),r(`div`,null,[...i[0]||=[n(`<h1 id="troubleshooting-build-errors" tabindex="-1"><a class="header-anchor" href="#troubleshooting-build-errors"><span>Troubleshooting Build Errors</span></a></h1><p>The build pipelines section on the package detail page reports the build status of each versioned Git tag, allowing you to check for any failed builds and their respective reasons. When a build fails, it leaves an error code and a message in the note field, providing valuable information for troubleshooting.</p><p><img src="/assets/package-detail-build-pipelines-WQGS-O0n.png" alt="build-pipelines"></p><p>OpenUPM categorizes these errors into two types: retryable errors and non-retryable errors.</p><h2 id="retryable-errors" tabindex="-1"><a class="header-anchor" href="#retryable-errors"><span>Retryable Errors</span></a></h2><p><strong>Retryable errors</strong> are typically caused by temporary network, registry, GitHub, or build-service conditions. OpenUPM automatically retries retryable release builds up to 3 times. If a retryable error remains after all attempts, the release is retained as failed so an operator can inspect or requeue it.</p><p>For <code>trackingMode: githubRelease</code>, missing GitHub Releases and missing publishable assets can recover after the initial attempts are exhausted. OpenUPM checks again after 10 minutes, then 20 minutes, then 40 minutes, doubling until the interval reaches 6 hours. It then checks every 6 hours for up to 3 days from the first missing-release or missing-asset failure.</p><table><thead><tr><th>Error</th><th>Reason</th><th>What to do</th></tr></thead><tbody><tr><td><code>E0</code> unknown error</td><td>OpenUPM could not classify the build failure.</td><td>Open the linked build logs and inspect the failed step. If the logs do not point to a package-side problem, report it with the build ID.</td></tr><tr><td><code>E400</code> registry bad request</td><td>The registry rejected a publish request as invalid.</td><td>Check the linked build logs for the registry response. If package metadata looks valid, report it so the registry-side request can be inspected.</td></tr><tr><td><code>E401</code> registry unauthorized</td><td>The registry rejected the publish request because authentication failed.</td><td>This is usually an OpenUPM registry credential or token issue. Report it with the build ID.</td></tr><tr><td><code>E403</code> registry forbidden</td><td>The registry rejected the publish request because access was forbidden.</td><td>This is usually an OpenUPM registry permission issue. Report it with the build ID.</td></tr><tr><td><code>E500</code> registry internal error</td><td>The registry returned an internal server error.</td><td>Wait for the OpenUPM pipeline retry. If repeated builds fail with <code>E500</code>, report it with the build ID.</td></tr><tr><td><code>E502</code> upstream bad gateway</td><td>A temporary upstream gateway error interrupted the build or publish request.</td><td>Wait for the OpenUPM pipeline retry after the upstream service recovers. If repeated builds fail with <code>E502</code>, report it with the build ID.</td></tr><tr><td><code>E503</code> upstream service unavailable</td><td>A required upstream service was temporarily unavailable.</td><td>Wait for the OpenUPM pipeline retry. If repeated builds fail with <code>E503</code>, report it with the build ID.</td></tr><tr><td><code>E504</code> upstream timeout</td><td>A temporary upstream timeout interrupted the build or publish request.</td><td>Wait for the OpenUPM pipeline retry after the upstream service recovers. If repeated builds fail with <code>E504</code>, report it with the build ID.</td></tr><tr><td><code>E700</code> build timed out</td><td>The Azure build did not finish before OpenUPM stopped waiting for it. The build may still finish later, and a later pipeline retry can succeed.</td><td>Wait for the OpenUPM pipeline retry or check the linked Azure build logs. If repeated builds fail with the same reason, report it with the build ID.</td></tr><tr><td><code>E701</code> build cancelled manually</td><td>The Azure build was cancelled.</td><td>Requeue or retag when the cancellation was accidental. If cancellation repeats, report it with the build ID.</td></tr><tr><td><code>E900</code> connection timeout</td><td>The build could not reach a required network service.</td><td>Wait for the OpenUPM pipeline retry. If repeated builds fail with <code>E900</code>, check whether the source repository, Git LFS, GitHub Release asset, or registry endpoint is reachable.</td></tr><tr><td><code>E904</code> GitHub Release not found</td><td>For <code>trackingMode: githubRelease</code>, the configured tag did not have a matching GitHub Release.</td><td>Create the GitHub Release for the tag. OpenUPM checks again after 10 minutes, then 20 minutes, then 40 minutes, doubling to 6 hours and continuing every 6 hours for up to 3 days. After that, request a rebuild.</td></tr><tr><td><code>E905</code> GitHub Release asset not found</td><td>For <code>trackingMode: githubRelease</code>, the GitHub Release exists but no publishable <code>.tgz</code> or <code>.tar.gz</code> asset matched the package metadata.</td><td>Upload the package tarball to the GitHub Release. OpenUPM checks again after 10 minutes, then 20 minutes, then 40 minutes, doubling to 6 hours and continuing every 6 hours for up to 3 days. After that, request a rebuild.</td></tr><tr><td><code>E906</code> multiple matching GitHub Release assets</td><td>More than one GitHub Release asset matched the package selection rules.</td><td>Keep exactly one publishable package tarball, or set <code>githubReleaseAssetName</code> to a stable filename or prefix. Unlike <code>E905</code>, this is not expected to resolve by uploading a missing asset later; update the GitHub Release asset list or package metadata, then request a rebuild.</td></tr><tr><td><code>E907</code> GitHub Release API error</td><td>GitHub&#39;s API failed while OpenUPM was looking up the release or asset.</td><td>Wait for the OpenUPM pipeline retry. If repeated builds fail with <code>E907</code>, report it with the build ID.</td></tr><tr><td><code>E908</code> GitHub Release asset download failed</td><td>The selected GitHub Release asset could not be downloaded in the Azure build.</td><td>Check that the asset is still attached, public, and downloadable. Replace the asset or request a rebuild after GitHub recovers.</td></tr></tbody></table><h2 id="non-retryable-errors" tabindex="-1"><a class="header-anchor" href="#non-retryable-errors"><span>Non-retryable Errors</span></a></h2><p><strong>Non-retryable errors</strong> are usually caused by package metadata, source repository, versioning, or repository-size issues. A rebuild will usually fail the same way until the package owner changes the source repository, package metadata, or release tag.</p><table><thead><tr><th>Error</th><th>Reason</th><th>What to do</th></tr></thead><tbody><tr><td><code>E409</code> version already exists</td><td>The package version has already been published. This often happens when a new Git tag points to a package.json version that was already released.</td><td>Update the <code>version</code> field in package.json, commit it, and create a new tag. Do not reuse an already published package version.</td></tr><tr><td><code>E413</code> package is larger than 512MB</td><td>The packed package is larger than the registry limit.</td><td>Remove generated files, caches, large binaries, or other unnecessary content from the package. Use <code>.npmignore</code> or the <code>files</code> field in package.json to control what gets packed.</td></tr><tr><td><code>E800</code> error path package.json</td><td>The build could not find a package.json with the expected package name.</td><td>Make sure the tagged commit contains package.json, the package name matches the OpenUPM metadata, and the package is under the expected path. If package.json was added after the tag, create or move the tag to the correct commit.</td></tr><tr><td><code>E801</code> package is explicitly private</td><td>package.json has <code>&quot;private&quot;: true</code>.</td><td>Remove <code>&quot;private&quot;: true</code> for public packages, commit the change, and create a new tag.</td></tr><tr><td><code>E803</code> package name is invalid</td><td>The package name is not valid for OpenUPM.</td><td>Use a reverse-DNS Unity package name such as <code>com.company.package</code>. Scoped npm names such as <code>@scope/name</code> are not supported as UPM package names.</td></tr><tr><td><code>E804</code> invalid package.json format</td><td>package.json could not be parsed or is missing required fields.</td><td>Validate package.json, make sure it is valid JSON, and include required fields such as <code>name</code> and <code>version</code>.</td></tr><tr><td><code>E805</code> remote branch or tag not found</td><td>The configured Git tag or branch was not found in the source repository.</td><td>Confirm the tag exists on the remote repository and matches the OpenUPM release metadata. Push the missing tag or create a new valid tag.</td></tr><tr><td><code>E806</code> invalid package version</td><td>The package.json <code>version</code> field is not a valid version string for publishing.</td><td>Update package.json to a valid semantic version, commit it, and create a new tag.</td></tr><tr><td><code>E807</code> remote repository unavailable</td><td>The source repository could not be reached. It may be deleted, renamed, unavailable, or private.</td><td>Make sure the repository URL is correct and public. Update OpenUPM metadata if the repository moved.</td></tr><tr><td><code>E808</code> remote submodule repository unavailable</td><td>A Git submodule repository could not be reached.</td><td>Make each required submodule public and reachable, or remove the submodule from the package source.</td></tr><tr><td><code>E809</code> submodule commit cannot be fetched</td><td>A referenced submodule commit is missing or inaccessible.</td><td>Push the missing submodule commit to its remote, update the submodule pointer, and create a new tag.</td></tr><tr><td><code>E810</code> npm publish lifecycle hook failed</td><td>An npm lifecycle hook such as <code>prepack</code>, <code>prepare</code>, or <code>postpack</code> failed during package packing or publishing.</td><td>Fix or remove the failing lifecycle script. OpenUPM packages should not depend on unpublished local build tooling during publish.</td></tr><tr><td><code>E811</code> package.json version does not match release version</td><td>The package.json version in the selected source does not match the version OpenUPM discovered from the Git tag or GitHub Release.</td><td>Move or recreate the Git tag or GitHub Release so it matches the package.json version. For example, package.json version <code>1.0.0</code> should use a matching release tag such as <code>v1.0.0</code> or <code>1.0.0</code>.</td></tr><tr><td><code>E901</code> heap out of memory</td><td>The package build ran out of memory. Large package contents are a common cause.</td><td>Reduce package size, remove generated/cache files from the packed package, or split very large assets into a different delivery path.</td></tr><tr><td><code>E902</code> Git LFS budget exceeded</td><td>Git LFS refused the download because the repository exceeded its bandwidth quota.</td><td>Restore Git LFS bandwidth or remove required LFS objects from the package source, then request a rebuild.</td></tr><tr><td><code>E903</code> Git LFS object is missing</td><td>A Git LFS pointer exists, but the referenced object is missing from the remote server.</td><td>Upload the missing LFS object or remove the broken pointer, then create a new tag.</td></tr></tbody></table><h2 id="how-to-trigger-a-rebuild-for-a-failed-build" tabindex="-1"><a class="header-anchor" href="#how-to-trigger-a-rebuild-for-a-failed-build"><span>How to Trigger a Rebuild for a Failed Build</span></a></h2><p>So you&#39;ve encountered a failed build and successfully fixed the issue. How do you trigger a rebuild? The solution is to re-tag the existing Git tag.</p><p>When the build pipelines detect a failed Git tag that has been re-tagged, it will initiate a rebuild. It&#39;s important to note that the build pipelines will not rebuild an already successful release, even if they detect that the Git tag has been removed or re-tagged. This is because what has been released on the registry is immutable.</p><p>To re-tag an existing Git tag, you can follow these steps:</p><ol><li>Locate the Git tag on the GitHub website.</li><li>Delete the existing tag.</li><li>Create a new tag with the same name from the latest commit.</li></ol><p>Alternatively, you can use the following commands to re-tag a Git tag:</p><div class="language-bash" data-highlighter="prismjs" data-ext="sh"><pre><code class="language-bash"><span class="line"><span class="token comment"># List all remote tags</span></span>
<span class="line"><span class="token function">git</span> ls-remote <span class="token parameter variable">--tags</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># Delete the local tag</span></span>
<span class="line"><span class="token function">git</span> tag <span class="token parameter variable">-d</span> v1.0.1</span>
<span class="line"></span>
<span class="line"><span class="token comment"># Push the tag deletion to the remote</span></span>
<span class="line"><span class="token function">git</span> push origin :refs/tags/v1.0.1</span>
<span class="line"></span>
<span class="line"><span class="token comment"># Tag the local branch again</span></span>
<span class="line"><span class="token function">git</span> tag v1.0.1</span>
<span class="line"></span>
<span class="line"><span class="token comment"># Push the tag to the remote</span></span>
<span class="line"><span class="token function">git</span> push origin tag v1.0.1</span>
<span class="line"></span></code></pre></div><p>Some repositories adhere to an immutable policy that disallows re-tagging. In such cases, you can trigger a new build by incrementing the package version. For instance, if the erroneous Git tag is <code>v1.0.1</code>, you should update the package version to <code>1.0.2</code> and then create a new Git tag, <code>v1.0.2</code>, to initiate a new build.</p>`,19)]])}var s=t(a,[[`render`,o]]);export{i as _pageData,s as default};